# Optional. Vite exposes only VITE_* variables.
#
# Dev default: if unset, src/api/client.js uses http://127.0.0.1:5000 (see client.js).
# Override if your API runs elsewhere:
# VITE_API_URL=http://127.0.0.1:5000
